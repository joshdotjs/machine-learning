# Create it:
python3 -m venv venv

# Activate it:
source venv/bin/activate  # For Mac/Linux

# Was it activated?
which python
which python3

# Install libs:
pip install numpy
pip install matplotlib
pip install scipy
pip install scikit-learn